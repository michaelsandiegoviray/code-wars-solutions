/*
Write a function to convert a name into initials. This kata strictly takes two words with one space in between them.

The output should be two capital letters with a dot separating them.

It should look like this:

Sam Harris => S.H

patrick feeney => P.F


*/














// function abbrevName(name) {

//     let divided = name.split(' ',);
//     // splitting the name into two parts of the array or multiple parts of an array 
//     let peace = divided.map(element => element[0].toUpperCase())
//     // so we are mapping the divided array and from each element we are taking the 0th character and putting it to uppercase 
//     return `${peace[0]}.${peace[1]}`
//     // we are then returning the peace array as a template string to represent initials 
// }


