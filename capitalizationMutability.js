/*


function capitalizeWord(word) {
  let total = word.replace(word[0],word[0].toUpperCase());
  
  return total
;
}


*/

function capitalizeWord(word) {
    let total = word.replace(word[0], word[0].toUpperCase());

    return total
        ;
}